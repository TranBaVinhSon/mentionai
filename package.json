{
  "name": "mentionai",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": {
    "packages": [
      "apps/*"
    ]
  },
  "scripts": {
    "dev": "yarn workspaces run dev",
    "dev:backend": "yarn workspace gengar start:dev",
    "dev:frontend": "yarn workspace gengar_fe dev",
    "build": "yarn workspaces run build",
    "build:backend": "yarn workspace gengar build",
    "build:frontend": "yarn workspace gengar_fe build",
    "start": "yarn workspaces run start",
    "start:backend": "yarn workspace gengar start:prod",
    "start:frontend": "yarn workspace gengar_fe start",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test",
    "clean": "rm -rf apps/*/node_modules apps/*/.next apps/*/dist node_modules"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
